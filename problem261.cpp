/* minimum total characters that must be deleted to make the 
strings anagrams (same strings). */

#include<iostream>
using namespace std;
bool is_anagram(string a, string b)
{
    if(a.length() != b.length())
        return false;
    else
    {
        // count freq. of each char in both strings then compare
        
    }
    
}
int main()
{
    string a, b;
    cin>>a>>b;
    while(!is_anagram(a, b))
    {
        // if size or count of a char in any str is more, cut its size
        
    }
    return 0;
}